[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "accelerate>=1.12.0",
    "numpy>=2.2.6",
    "pydantic>=2.12.5",
    "torch>=2.9.1",
    "transformers>=4.57.5",
]
